name: Release notes

on:
  push:
    branches:
      - main
    paths-ignore:
      - .release-notes/next-release.md

jobs:
  release-notes:
    name: Release notes
    uses: ponylang/shared-workflows/.github/workflows/release-notes-bot-next-release.yml@main
    secrets:
      token: ${{ secrets.PONYLANG_TOKEN }}
