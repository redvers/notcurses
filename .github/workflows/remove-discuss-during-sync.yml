name: Remove discuss during sync label

on:
  issues:
    types: [closed]
  pull_request:
    types: [closed]

jobs:
  remove-label:
    name: Remove discuss during sync
    uses: ponylang/shared-workflows/.github/workflows/remove-discuss-during-sync-label.yml@main
    secrets:
      token: ${{ secrets.PONYLANG_TOKEN }}
